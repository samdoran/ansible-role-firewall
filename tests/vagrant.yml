- hosts: all
  remote_user: vagrant
  become: yes

  vars:
    firewall_advanced_rules:
      - source: '10.0.1.17'
        protocol: 'tcp'
        dest_port: 22
        new: True

  roles:
    - firewall
